# Django Backend
SECRET_KEY=your-super-secret-key-change-in-production
DEBUG=False

# Database
POSTGRES_DB=funzone_db
POSTGRES_USER=funzone_user
POSTGRES_PASSWORD=funzone_password

# MongoDB
MONGO_USER=mongo_user
MONGO_PASSWORD=mongo_password

# API Base URL (for frontend builds)
VITE_API_BASE_URL=http://localhost/api

# Development API URL (for dev docker-compose)
# VITE_API_BASE_URL=http://localhost:8888/api

# Django ALLOWED_HOSTS (comma-separated list, will be updated with server IP by setup script)
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,backend,frontend-customer,frontend-owner,nginx

